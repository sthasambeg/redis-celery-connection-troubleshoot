services:
  default-worker:
    container_name: rct-default
    build:
      context: ./apps/default
      dockerfile: Dockerfile
    cap_add:
      - NET_ADMIN
    environment:
      - CELERY_APP=main:app
      - CELERY_LOGLEVEL=info
      - CELERY_CONCURRENCY=1
      - CELERY_QUEUES=connection_test__default
      - CELERY_HOSTNAME=default-worker@%h
    env_file:
      - .env
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  updated-settings-worker:
    container_name: rct-settings
    build:
      context: ./apps/updated_settings
      dockerfile: Dockerfile
    cap_add:
      - NET_ADMIN
    environment:
      - CELERY_APP=main:app
      - CELERY_LOGLEVEL=info
      - CELERY_CONCURRENCY=1
      - CELERY_QUEUES=connection_test__default
      - CELERY_HOSTNAME=updated-settings-worker@%h
    env_file:
      - .env
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  updated-package-worker:
    container_name: rct-package
    build:
      context: ./apps/updated_package
      dockerfile: Dockerfile
    cap_add:
      - NET_ADMIN
    environment:
      - CELERY_APP=main:app
      - CELERY_LOGLEVEL=info
      - CELERY_CONCURRENCY=1
      - CELERY_QUEUES=connection_test__default
      - CELERY_HOSTNAME=updated-package-worker@%h
    env_file:
      - .env
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

networks:
  default:
    name: redis-celery-network
